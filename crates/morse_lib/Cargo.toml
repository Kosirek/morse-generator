[package]
name = "morse_lib"
version = "0.1.0"
edition = "2021"

[dependencies]
rodio = "0.21.1"
once_cell = "1.21.3"

[profile.release]
opt-level = 3
debug = true
codegen-units = 1
lto = "fat"
panic = "abort"
incremental = false
strip = "symbols"
